# Dev Log — C3 (Ayim Obed Boateng)

## Week 2 — 2026-08-14

**Done:**

* Pulled the latest `develop` branch and continued work on `feature/c3-disjoint-set-kruskal`.
* Received and preserved the frozen C3 interfaces supplied by A1 for `DisjointSet` and `Kruskal`.
* Implemented the array-backed Disjoint Set using String location IDs without banned Java collection structures.
* Implemented `makeSet()`, `find()` with path compression, `union()` with union by rank, `connected()`, `findWithTrace()`, and `setCount()`.
* Implemented Kruskal's Minimum Spanning Tree algorithm using the shared `Graph` interface and the C3 Disjoint Set for cycle detection.
* Used the project effective-cost rule `travelTimeSecs × roadConditionWeight` when comparing road/corridor edges.
* Added normal, boundary, and invalid-input JUnit tests for both the Disjoint Set and Kruskal.
* Added trace-capable tests for Disjoint Set and a Kruskal MST demonstration trace.
* Verified the complete Maven test lifecycle in IntelliJ with `BUILD SUCCESS`.
* Generated a Kruskal demonstration trace selecting three MST edges with effective costs `5.0`, `8.0`, and `10.0`, giving a total cost of `23.0`.
* Opened Pull Request #11 from `feature/c3-disjoint-set-kruskal` into `develop`.
* Drafted the C3 report section in `report/sections/c3-disjoint-set-kruskal.md`.

**What broke:**

* The local PowerShell terminal initially could not run `mvn test` because Maven was not available as a command on the Windows PATH. IntelliJ initially had the Maven tool window disabled as well. The project's `pom.xml` was loaded as a Maven project and the Maven lifecycle was then run successfully through IntelliJ.
* The first attempt to push the feature branch failed because GitHub HTTPS password authentication is no longer accepted. GitHub CLI was not installed on the machine. GitHub CLI was installed and authenticated, after which the feature branch pushed successfully.
* The C3 feature branch had originally been created before the frozen C3 interface commit reached `develop`. The latest `develop` commit was merged into the C3 branch before implementation so that the correct A1 contracts were used.
* The final required trace must come from the team's real hospital dataset. The current four-node hospital graph is therefore being treated only as demonstration/trace-mechanism evidence and not falsely labelled as the final real-dataset trace.

**Decided:**

* Keep all frozen C3 public signatures unchanged so downstream components can compile against the agreed interface.
* Use only arrays for the Disjoint Set's assessed core storage rather than `HashMap` or other prohibited Java collections.
* Use union by rank together with path compression to keep the Disjoint Set parent forest shallow.
* Keep String IDs externally because this matches the shared graph and hospital location contracts.
* Use the team's defined effective road cost, `travelTimeSecs × roadConditionWeight`, consistently in Kruskal.
* Clearly distinguish demonstration trace output from final real-dataset evidence.
* Final C3 real-dataset MST evidence remains pending until the team's database/seed road data is available.

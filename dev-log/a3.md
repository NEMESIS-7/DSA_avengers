# Dev Log — A3 (Kena-Bonti Gabriel)

## Week 2 — 2026-08-15

**Done:**
- Confirmed the null-check and LIFO test fixes from pod lead's PR #4 review are live after
  pulling — `Stack.java`'s `push()` throws on null, `StackTest.java` has all 5 tests (normal,
  boundary, invalid, null-push, LIFO order).
- Added `road_name` to `roads` and `patient_ref` to `service_requests`, both requested at this
  week's in-person meeting. `patient_ref` only applies to the 8 patient-facing categories
  (REFERRAL_IN/OUT, PATIENT_TRANSFER, SPECIMEN, DRUG_DELIVERY, BLOOD, MORTUARY_TRANSFER, MEALS) —
  left NULL for STERILE_SUPPLY, EQUIPMENT, LINEN, MAINTENANCE, since those aren't tied to a
  specific patient.
- Generated `patient_ref` so patients realistically repeat across requests rather than a flat
  1:1 mapping — 144 unique synthetic patients across 245 patient-eligible rows, 71 of them with
  2–4 requests, so B4's hash table has genuine collision/chaining cases to demonstrate.
- Updated `DatabaseLoader.java` to load both new columns, and fixed the hardcoded Postgres
  password to read from `DB_PASSWORD` again instead of a plaintext value.
- Wrote `docs/evidence/a3-trace-tables.md` — array-level trace of `push()`/resize through the
  boundary case, plus a functional trace of the full undo-dispatch flow on real REQ-0001 data.
- Rewrote `data-provenance.md` to also document how `patient_ref` was generated.
- Updated PR #4's description — all 5 Definition of Done items now checked, the trace table
  closes out the "unsure about" item from the original description.
- Manually verified the layer-join integrity property against the real dataset: 7 roads cross
  between EXTERNAL and INTERNAL, all 7 touch the gateway node, zero bypass it.

**What broke:**
- `sql/seed/service_requests_seed.sql` was never updated when `patient_ref` was added — the
  `INSERT`'s column list didn't include it, so anyone provisioning their local DB via this seed
  script instead of the CSV got every row back with `patient_ref = NULL`. This is very likely
  what a teammate hit when they said they couldn't see the new column after I'd already pushed
  and merged. Fixed by regenerating all 300 rows with the identical `patient_ref` mapping already
  live in the merged CSV/migration, so nothing already merged gets contradicted.
- `patient_ref` was generated using `randomSeed = 6316`, self-computed at kickoff from all 15
  index numbers, pending official confirmation at the time. A1's Week 2 log claimed the official
  S was 536 — but recomputing directly from the actual roster, using the documented formula (sum
  of the last three digits of every index number), gives 6316, matching what I already had. Flagged
  the discrepancy to A1 directly, since `Config.java` (which 8 already-merged slots pull from) was
  built on the 536 figure. No change needed on my end — `patient_ref` was correct from the start.
- Noted A1 also had to fix a package mismatch in my Stack files (`gsoo.structures` instead of
  `gsoo.structures.a3_stack`, colliding with where `Graph` lives) and split `DatabaseLoader`'s
  model classes into their own public files. Need to pull `develop` before my next push so I'm
  not working against a stale local copy of my own files.

**Decided:**
- Sequenced the fixes above: seed script gap first, since it's actively blocking a teammate
  right now; the randomSeed correction second, since nothing is currently depending on it being
  exactly right and reseeding carelessly right before a meeting risks creating a third
  inconsistent version of the same data.

**Still outstanding:**
- DFS not started — waiting on C5's adjacency matrix before building against just one graph
  implementation.
- Layer-join integrity test verified manually, but not yet written up as a proper joint artifact
  with C4.
